# 수동 테스트 시나리오

실제로 클릭이 나가는 경로는 자동 테스트로 덮을 수 없어(전역 단축키·화면 좌표·Robot 클릭)
손으로 확인한다. 아래는 2026-09-21~22 변경분을 기준으로 한 목록이다.

★ 표시는 한 번이라도 돌려야 하는 것, 나머지는 여유 있을 때.

## 준비

- **클릭 대상은 메모장·그림판처럼 눌려도 아무 일 없는 창**을 쓴다. 빈 바탕화면도 좋다.
- **실제 예매·결제 사이트에서 시험하지 않는다.** 기준 사이트로도 쓰지 않는다.
- 기준 사이트는 기본값(`map.naver.com`)이나 `www.google.com` 정도면 충분하다.
- 예약 대기 중에는 앱이 전역 `Ctrl + F1`을 잡고 있다. 다른 작업과 겹치지 않을 때 한다.

---

## A. 한 바퀴 돌려보기 ★

가장 중요한 시나리오. 나머지는 이게 되고 난 다음이다.

| # | 하는 일 | 기대 |
|---|---|---|
| A1 | 앱 실행 | 주소 칸이 `https://map.naver.com/p/` 로 바뀐다(리다이렉트 승격). 시계가 청록색 |
| A2 | 30초쯤 지켜본다 | `갱신 N초 전` 숫자가 1초씩 오르다 30초 근처에서 0으로 돌아간다 |
| A3 | 그래프를 본다 | 동기화마다 점이 하나씩 늘고 `최근 N회 · 폭 N ms` 가 갱신된다 |
| A4 | 메모장 위 두 곳에서 `Ctrl + F1` | 목록에 `1.`, `2.` 로 쌓이고 라벨이 `좌표 2개 지정됨` |
| A5 | 목표 시각을 **현재 + 1분**으로 입력, `예약 시작` | 카운트다운이 돌고 단계 줄에 `목표 시각까지 대기 중` |
| A6 | 목표 5초 전 | `목표 직전 정밀 동기화 중...` → `정밀 동기화 완료. 클릭 시각 대기 중` |
| A7 | 목표 시각 | `클릭 1/2` → `클릭 2/2`. 메모장에 실제로 커서가 옮겨가며 눌린다 |
| A8 | 완료 직후 | 결과에 `클릭 2회 완료: 첫 클릭 목표 대비 +NN ms`. 버튼·입력이 모두 풀린다 |
| A9 | 목표 시각 칸을 본다 | **다음 30분 정각으로 다시 채워져 있다** (지나간 시각이 남아 있으면 안 된다) |
| A10 | 곧바로 `예약 시작` 다시 | 카운트다운이 **몇 십 분** 이내다. 1400분대가 나오면 내일로 잡힌 것이라 결함 |

## B. 예약 취소 ★

| # | 하는 일 | 기대 |
|---|---|---|
| B1 | 예약 걸고 바로 `예약 취소` | `예약을 취소했습니다. 클릭하지 않았습니다.` 카운트다운·단계 줄이 지워진다 |
| B2 | 취소 후 좌표·간격·시각을 바꿔본다 | 전부 다시 만져진다 |
| B3 | 취소 후 목표 시각 칸 | **입력했던 값 그대로다.** 취소는 목표를 빼앗지 않는다 |
| B4 | 예약 걸고 **목표 5초 전 정밀 동기화 중**에 취소 | 곧바로 멈춘다. 마우스가 첫 좌표로 움직이지 않는다 |
| B5 | 클릭이 시작된 뒤 취소 | `이미 나간 클릭은 되돌릴 수 없습니다`. 마우스 왼쪽 버튼이 눌린 채 남지 않는다(드래그되지 않음) |

## C. 예약 중에는 잠긴다 ★

예약은 시작 시점의 값으로 진행된다. 화면이 그 사실과 어긋나지 않는지 본다.

| # | 하는 일 | 기대 |
|---|---|---|
| C1 | 예약 대기 중 `Ctrl + F1` | 좌표가 늘지 않는다. 라벨은 `예약에 좌표 N개가 고정되었습니다` |
| C2 | `마지막 취소`·`좌표 초기화`·클릭 간격·목표 시각·`다음 30분 자동 입력`·주소 칸·`적용`·`인증서 검증 완화` | 전부 비활성 |
| C3 | 주소 칸에 커서를 두고 Enter | 아무 일도 일어나지 않는다(버튼이 꺼져 있어도 Enter는 들어올 수 있어 따로 막았다) |
| C4 | `예약 시작` 버튼 | 비활성이고 글자가 `예약 대기 중` |

## D. 입력 검증

| # | 하는 일 | 기대 |
|---|---|---|
| D1 ★ | 목표 시각을 **현재 + 5초**로 넣고 시작 | `목표 시각까지 10초도 남지 않았습니다...` 로 **거부**. 말없이 내일로 가면 결함 |
| D2 | 목표 시각 `25:00:00` | `HH:mm:ss 형식으로 입력하세요` |
| D3 | 목표 시각을 `abc`, 클릭 간격도 `abc` 로 동시에 | **시각 오류**가 뜨고 포커스가 시각 칸으로 간다(간격 오류가 덮지 않는다) |
| D4 | 클릭 간격 `5` | `20 ~ 10000 ms 사이` 안내 |
| D5 | 목표 시각을 **현재 - 1분**으로 | 내일로 잡힌다(정상). 카운트다운이 1400분대 |

## E. 기준 사이트

| # | 하는 일 | 기대 |
|---|---|---|
| E1 ★ | `nonexistent-xyz.example` 적용 | 실패 문구가 뜨고 **주소 칸이 원래 주소로 되돌아간다** |
| E2 ★ | E1 직후 30초 기다린다 | 상태 줄이 초록으로 돌아와도 주소 칸과 어긋나지 않는다. 있지도 않은 사이트에 맞춰진 것처럼 보이면 결함 |
| E3 | 목표 시각을 **직접 입력**한 뒤 주소를 바꿔 적용 | **입력한 시각이 그대로 남는다**(앱이 덮어쓰지 않는다) |
| E4 | `다음 30분 자동 입력` 누른 뒤 주소 적용 | 이번에는 다음 30분으로 다시 채워진다(앱이 채운 값이므로) |
| E5 | `www.google.com` 적용 | 그래프가 비워지고 점이 처음부터 다시 쌓인다(다른 서버 측정치를 섞지 않는다) |

## F. 동기화가 끊길 때

| # | 하는 일 | 기대 |
|---|---|---|
| F1 | Wi-Fi를 끄고 45초 이상 기다린다 | `갱신이 끊겼습니다` 가 빨간 글씨로 붙는다 |
| F2 ★ | Wi-Fi를 다시 켠다 | 다음 주기에 갱신이 재개되고 **`예약 시작` 버튼이 다시 풀린다**(잠긴 채 남으면 결함) |
| F3 | 그래프에 붉은 점이 생기면 | 캡션에 `경계 놓침 N회`. 그 회차 상태 줄에 `초 경계 못 잡음(±500ms)` |

## G. 목표 직전에 네트워크가 끊길 때 ★

한 번뿐인 예약이 네트워크 오류 하나로 날아가지 않는지 보는 시나리오.

| # | 하는 일 | 기대 |
|---|---|---|
| G1 | 예약을 걸고 **목표 10초 전**에 Wi-Fi를 끈다 | 단계 줄에 `정밀 동기화 실패 — 직전에 맞춰 둔 기준으로 진행합니다` |
| G2 | 그대로 둔다 | **클릭이 그대로 나간다.** 아무것도 안 하고 끝나면 결함 |
| G3 | 완료 문구 | `... | 목표 직전 정밀 동기화에 실패해 이전 기준으로 클릭했습니다` 가 붙고 글자가 노란색 |

## H. 종료

| # | 하는 일 | 기대 |
|---|---|---|
| H1 | 예약 대기 중 창을 닫는다 | 창이 닫히고 `java` 프로세스가 남지 않는다 |
| H2 | 화면을 잠갔다 풀고 `Ctrl + F1` | 좌표가 정상으로 잡힌다(포인터를 못 읽는 순간에 조용히 죽지 않는다) |

---

## 기록할 것

A7의 `목표 대비 +NN ms` 값을 몇 번 적어 두면 쓸모가 있다. 안전 지연(현재 45~160 ms 사이에서
RTT에 따라 정해짐)만큼 뒤에 눌리는 것이 정상이고, 그보다 크게 벗어나면 그때 그래프의 폭과
`경계 놓침` 횟수를 같이 본다.
